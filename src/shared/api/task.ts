import * as CancelBuildArtifact$ApplicationId$ArtifactCommitIdInMachine$ServerId from './task.cancel.build-artifact-$applicationId-$artifactCommitId-in-machine-$serverId';

const Root = {
  Cancel: {
    BuildArtifact$ApplicationId$ArtifactCommitIdInMachine$ServerId:
      CancelBuildArtifact$ApplicationId$ArtifactCommitIdInMachine$ServerId,
  },
};

export default Root;
