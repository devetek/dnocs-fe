import * as Create from './database.create';
import * as Delete from './database.delete.$id';
import * as Find from './database.find';

const Root = {
  Create,
  Delete,
  Find,
};

export default Root;
